services:
  - type: web
    name: shubhviah-digital-invitation
    env: static
    buildCommand: npm install && npm run build
    publishDir: dist
    headers:
      - path: /*
        name: Cache-Control
        value: public, max-age=31536000, immutable
